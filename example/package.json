{
  "private": true,
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "spago build -u '+RTS -N2 -RTS' && webpack --mode production --progress",
    "start": "webpack-dev-server --mode development --progress"
  },
  "devDependencies": {
    "html-webpack-plugin": "^3.2.0",
    "webpack": "^4.30.0",
    "webpack-cli": "^3.3.1",
    "webpack-dev-server": "^3.3.1"
  }
}
