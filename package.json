{
  "scripts": {
    "build": "spago build -u '--strict --stash +RTS -N2 -RTS'",
    "test": "spago build && jest"
  },
  "devDependencies": {
    "jest": "^28.1.0",
    "jest-environment-jsdom": "^28.1.0"
  }
}
